export interface PreArrival {
  id: string;
  otp: string;
  tracking_no: string;
  user: string;
  suite: string;
  estimate_arrival_time: string;
  created_at: string;
  status: string;
  details?: string;
}
